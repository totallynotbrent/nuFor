# Architecture

Module boundaries, the Rust/Fortran FFI boundary, and data flow. The FFI
boundary definition and decision live in `docs/research/ffi-boundary.md`.
Entries land as subsystems are built.
