[package]
name = "nufor-config"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "case.toml schema and validation: the reproducibility root for a nuFor case (spec 40)"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[lints]
workspace = true

[dev-dependencies]
tempfile = "3"